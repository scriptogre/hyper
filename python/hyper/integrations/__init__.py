"""Hyper integrations for popular Python web frameworks.

Each submodule is an optional integration that pulls in framework-specific
dependencies. Import the one you need:

    from hyper.integrations.jinja2 import HyperExtension
    # or use hyper.integrations.django as a Django app via INSTALLED_APPS
"""
