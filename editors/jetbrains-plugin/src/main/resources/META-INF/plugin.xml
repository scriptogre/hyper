<idea-plugin>
    <id>com.hyper.plugin</id>
    <name>Hyper</name>
    <vendor>Hyper</vendor>
    <description><![CDATA[
        Support for Hyper template files (.hyper) - a language combining Python control flow with HTML.
    ]]></description>

    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <fileType
            name="Hyper"
            implementationClass="com.hyper.plugin.HyperFileType"
            language="Hyper"
            extensions="hyper"/>

        <lang.parserDefinition
            language="Hyper"
            implementationClass="com.hyper.plugin.HyperParserDefinition"/>

        <lang.syntaxHighlighterFactory
            language="Hyper"
            implementationClass="com.hyper.plugin.HyperSyntaxHighlighterFactory"/>

        <lang.commenter
            language="Hyper"
            implementationClass="com.hyper.plugin.HyperCommenter"/>

        <multiHostInjector implementation="com.hyper.plugin.HyperLanguageInjector"/>

        <!-- Auto-generate .py on save -->
        <vfs.asyncListener implementation="com.hyper.plugin.HyperFileListener"/>

        <!-- Debug tool window showing virtual injected files -->
        <toolWindow id="Hyper Inspector"
                    anchor="right"
                    factoryClass="com.hyper.plugin.HyperInspectorToolWindowFactory"
                    icon="/icons/hyper.svg"/>

        <!-- File nesting: .py files nest under their corresponding .hyper files -->
        <treeStructureProvider implementation="com.hyper.plugin.HyperFileNestingProvider"/>
    </extensions>
</idea-plugin>
